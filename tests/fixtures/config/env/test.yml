engines:

    test_engine:

        location: {'type': 'dev', 'path': '$SHOTGUN_TEST_ENGINE'}
        debug_logging: false

        apps:
            test_app:
                location: {'type': 'dev', 'path': '{CONFIG_FOLDER}/bundles/test_app'}

frameworks:
  tk-framework-shotgunutils_v4.x.x:
    location: {type: path, path: '$FRAMEWORK_ROOT' }
